class Coordinate {
  final double x;
  final double y;

  const Coordinate({
    required this.x,
    required this.y,
  });
}

class Circle {
  final double x;
  final double y;
  final double r;

  Circle(this.x, this.y, this.r);
}